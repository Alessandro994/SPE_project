upstream app {
# Repeat this block for each server
{{#servers}}
    server localhost:{{port}} {{{param}}};
{{/servers}}
{{#balancing_policy}}
    # Load balancing policy
    {{balancing_policy}};
{{/balancing_policy}}
}